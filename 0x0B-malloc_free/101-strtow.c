#include "main.h"
#include <stddef.h>
#include <stdlib.h>
/**
 * **strtow - Start of function that splits a string into words.
 * @str: pointer
 * Return: Null or word
 */
char **strtow(char *str)
{
	if (str == NULL || *str == "")
		return (NULL);
}
